# Supabaseのプロジェクト設定画面（Settings > API）にある値をここに貼り付けてください。
# このファイル自体は「ひな形」なので、実際の値は .env.local という名前の別ファイルにコピーして書き込んでください。
# .env.local はGitの管理対象から外れる想定のファイルなので、値をそのまま書いても安全です。

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
