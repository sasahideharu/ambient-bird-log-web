<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- 起動後のテーマ。ウィンドウの背景を黒にして、画面が描かれるまでの間に白く光らないようにする -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@color/ablBackground</item>
    </style>


    <!-- 起動画面（スプラッシュ）。背景を黒にし、アイコンは出さない（起動時に白く眩しくならないようにする） -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@color/ablBackground</item>
        <item name="android:windowBackground">@color/ablBackground</item>
        <item name="windowSplashScreenBackground">@color/ablBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_empty</item>
    </style>
</resources>
